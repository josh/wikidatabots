[project]
name = "wikidatabots"

[tool.pyright]

[tool.vulture]
exclude = ["*test_*.py"]
ignore_names = ["formatwarning", "status", "put_throttle", "maxlag"]

[tool.ruff.lint]
extend-select = ["I", "UP"]
